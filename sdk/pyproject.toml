[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scraperack"
version = "0.1.0"
description = "Run trusted Python functions on a self-hosted Ray cluster"
requires-python = ">=3.10"
dependencies = ["ray==2.58.0"]

[tool.setuptools.packages.find]
where = ["src"]
